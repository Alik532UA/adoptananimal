---
Назва: AGENTS.md — контекст проєкту adoptananimal
Опис: Канонічні інструкції для будь-якого AI-асистента в цьому репозиторії
---

# AGENTS.md — adoptananimal

> **Спершу прочитай [PROJECT-CONTEXT.md](PROJECT-CONTEXT.md).** Там персональний
> шар пакета v8: профіль і хостинг, сховище, обрані optional-файли, свідомі
> відхилення, відкриті питання й перелік автоматичних перевірок. Цей файл —
> коротка витримка для щоденної роботи.
>
> Загальні стандарти живуть у `sveltekit-canon/selection_criteria/v8`. Тут — лише
> те, що специфічне для цього репозиторію.

## Що це

Сайт прилаштування тварин: картки котів і собак, сторінка заявки, обране.
Чотири мови (`en` типова й без префікса, `uk`, `de`, `nl` — під префіксом), профіль
`static`, серверного рантайму немає — два наявні ендпоінти пререндеряться у файли.

## Архітектура

- **Фреймворк:** SvelteKit 2 + Svelte 5 (**виключно** руни), TypeScript 6.
- **Стан:** класи-сервіси в `.svelte.ts` (напр. `Settings` у `settings.svelte.ts`).
- **Сховище:** префікс `adoptananimal_` для `localStorage`, `sessionStorage`,
  Cache API та IndexedDB. Origin спільний із сусідніми проєктами.
  Фасад — `src/lib/services/storage.ts`, включно з `storage.session`.
- **i18n:** власна реалізація в `src/lib/i18n`, не `svelte-i18n`. Паритет ключів
  тримає `npm run check:i18n`.
- **Маршрути:** мовний префікс — опційний параметр `[[lang=lang]]`.
  Розділи: `adopt/cat`, `adopt/dog`, `adopt/{cat,dog}/[slug]`, `apply`,
  `apply/form`, `favorites`. `robots.txt` і `sitemap.xml` — теж маршрути.
  Поза індексом — `apply/form` і `beta-test-checklists`: перелік у `HIDDEN_ROUTES`
  (`src/lib/config.ts`), звідти макет, sitemap і `robots.txt` (PROJECT-CONTEXT § 4.22).
- **Стилі:** глобальні в `app.css`, базові в `src/lib/styles/base.css`,
  скіни й теми — `src/lib/styles/skins/` і `src/lib/styles/themes/`.
- **Дані тварин:** TypeScript-файли в `src/lib/data/animals/`.

## Локальні пастки

| Пастка                         | Що саме                                                                                                                                                                                                                                                                                                                            |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Порт dev-сервера — **5195**    | `.claude/launch.json` цього проєкту, зі `--strictPort`. Голий `npm run dev` натомість бере типовий порт Vite 5173, спільний із будь-яким сусіднім проєктом: якщо на ньому вже висить чужий dev-сервер, дивитись будеш чужий сайт. Тести цього не торкаються — Playwright піднімає власний `preview` на 4173, теж зі `--strictPort` |
| `base` виводиться з оточення   | `svelte.config.js` читає `process.env.BASE_PATH ?? ''`. Не хардкодь шлях                                                                                                                                                                                                                                                           |
| Абсолютні URL — не з `base`    | `SITE_ORIGIN` + `SITE_BASE` із `src/lib/config.ts`. `paths.relative: false`, тож `base` тепер теж абсолютний, але константа лишається незалежною від налаштування: повернути `relative` назад не має тихо змінювати сенс canonical (§ 4.38)                                                                                        |
| Сайт ще не задеплоєний         | GitHub Pages для цього репозиторію не налаштовано (`gh api repos/Alik532UA/adoptananimal/pages` повертає порожній `html_url`). Не посилайся на живу адресу, поки її немає                                                                                                                                                          |
| Джерело даних — зовнішній сайт | порядок тварин, імена, стать, вік, порода, розмір, колір, опис і статус адопції звіряються зі старим сайтом на Google Sites, а не вигадуються; адреса `adoptananimal.in.ua` тепер веде на ЦЕЙ сайт, тож старий шукати за його адресою Google Sites (PROJECT-CONTEXT § 5)                                                           |

## НЕ РОБИ

| Заборонено                                                               | Замість цього                                                                               |
| ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| Svelte 4 API: `writable`, `readable`, `on:click`, `<slot>`, `export let` | `$state` / `$derived`, `onclick`, `{@render …}`, `$props()`                                 |
| `localStorage.clear()`                                                   | вибіркове видалення за префіксом — origin спільний із сусідами                              |
| Прямий `localStorage.getItem(...)`                                       | фасад `storage` із `$lib/services/storage`                                                  |
| Фотографії із запеченими написами («I'm adopted!»)                       | чисті оригінали; статус `isAdopted: true` накладає локалізований стікер засобами CSS/Svelte |
| Хардкод текстів UI у шаблонах                                            | ключі i18n                                                                                  |
| Коміт без прогону перевірок                                              | спершу `npm run check` і `npm run lint`                                                     |

## Конвенції

- **`data-testid`:** обов'язковий для кожного інтерактивного елемента, шаблон
  `[scope-]<feature>-<role>-<type>` із суфіксами `-btn`, `-link`, `-input`,
  `-modal`, `-card`. Канон — TESTID-AND-NAMING-**v8**; рантайм-перевірка —
  `tests/testids.spec.ts`.
- **Гарячі клавіші:** `T` тема, `L` меню мов, `Esc` закрити, `V`/`R` — службові
  жести. Карта, пропущені літери й усі чотири захисти — у докблоці
  `src/lib/services/keyboard.ts`; гейт — `keyboard.test.ts`. Нову клавішу беруть
  із канонічної карти HOTKEYS-v8 § 1.1, а не вигадують.
- **CSS:** BEM-lite (`.block__element--modifier`).
- **Іменування:** компоненти `PascalCase.svelte` із суфіксом типу
  (`UserCard.svelte`), утиліти `camelCase.ts`, класи з рунами — `.svelte.ts`.
- **Доступність (WCAG 2.2 AA):** семантичний HTML, клавіатурна доступність,
  `aria-label` для кнопок-піктограм, підтримка `prefers-reduced-motion`,
  skip-link у `+layout.svelte`. Автоперевірка — `tests/a11y.spec.ts`.
- **Тести:** Vitest під `src/`, Playwright — **дванадцять** файлів у `tests/`
  (`a11y`, `beta-checklist`, `favorites`, `fluid-sizing`, `i18n`, `journey`, `scrollbar`,
  `skin-overrides`, `testids`, `toast`, `touch-targets`, `ui`). Playwright спершу робить
  `build`, тобто перевіряє **зібраний** сайт.
- **Вердикт `npm run lint` — це код виходу, а не рядок у виводі.** Скрипт це
  `npm run lint:format && npm run lint:code`; коли падає prettier, ESLint не
  запускається, і звичного `✖ N problems` у виводі немає взагалі. Тиша тут означає
  «до лінтера не дійшло», а не «чисто».
- **У CI це два окремі кроки** (`lint:format` і `lint:code`), кожен із
  `if: !cancelled()`, — щоб один невідформатований файл не з'їдав звіт ESLint, як
  сталося 2026-08-26. Локально послідовність лишається: там зручність важливіша за
  повноту звіту. Інваріант у `src/ci.test.ts` не дає повернути зчеплення в CI.

## Команди перевірки

```
npm run check        # svelte-check, має бути 0 помилок
npm run lint         # prettier --check + eslint, має бути 0 помилок
npm run check:discovery # чи раннери досі беруть усі 46 + 12 файлів перевірок
npm test             # юніт-інваріанти (Vitest)
npm run check:i18n   # паритет ключів у чотирьох мовах
npm run build        # збірка
npm run check:build  # гейт над build/
npm run check:bundle # бюджет JS: код і каталог тварин окремими числами
npm run test:e2e     # Playwright проти зібраного сайту
```

**Результат треба побачити, а не припустити.** Твердження «правило виконано»
робиться після прогону, а не замість нього. Частину дефектів цього проєкту видно
**лише** у `build/` — саме тому `check:build` існує окремо
(AI-AGENT-PITFALLS-v8 § 2).

## Карта проєктної документації

Канонічна документація — у `.private/docs/`. Наявні теки:

- `adr/` — рішення з архітектури. Канон.
- `specs/` — специфікації фіч. Канон.
- `analysis/` — одноразові звіти та аудити.
- `notes/` — робочі замітки (не канон).

При суперечності: `adr/` > `specs/` > `analysis/` > `notes/`.

Джерело істини для контенту описане в
`.private/docs/specs/0001-content-source-of-truth-and-assets.md`.
